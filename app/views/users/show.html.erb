<div class="contents row">
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @posts.each do |post| %>
    <%= render partial: "posts/post", locals: { post: post } %>
  <% end %>
  <div class="pagi-wrapper #8d6e63 brown" style="text-align: center;">
    <%= paginate(@posts) %>
  </div>
</div>
